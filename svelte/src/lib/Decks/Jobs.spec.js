import Jobs from './Jobs'
import Dice from '$lib/Dice';

it('holds a place until I can import Jobs without breaking jest', async () => {
  
});

it('it works', async () => {
	// Dice.primeWithSeed(20);
	// const job = Jobs.meleeBuffs();
	// expect(job.name).toBe('Melee Buffs');
});
